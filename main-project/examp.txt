main
var a, b, c, d, e; {
    let a <- call InputNum();
    let b <- a;
    let c <- b;
    let d <- b + c;
    let e <- a + b;
    if a < 0
        then
            let d <- d + e;
            let a <- d;
        else
            let d <- e;
    fi;
    call OutputNum(a);
}.



main
    var a; {
    let a <- call InputNum();

    if a > 0 then
        call OutputNewLine();
        else
        let a <- a + 2;
        call OutputNewLine();

    fi;

    call OutputNewLine();

    }.