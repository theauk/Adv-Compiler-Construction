main
    var a; {
    let a <- 2;

    if a > 0
    then
        if a > 0
        then
            let a <- a + 2;
        else
            let a <- a + 2;
        fi;
    else
        if a > 0
        then
            let a <- a + 2;
        else
            let a <- a + 2;
        fi;
    fi;

    }.