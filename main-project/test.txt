main
    var a, b; {

    let a <- 2;

    if a > 0 then
           let b <- 2 + a;
        else
        if a > 0 then
            let b <- 2 + a;
            else
            let b <- 2 + a;
            fi;

    fi;

    call OutputNewLine();

    }.