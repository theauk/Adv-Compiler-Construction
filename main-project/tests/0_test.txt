main
array [10] nums, prefix;
var i;
var last;

{
    let i <- 0;
    let last <- 0;
    while i < 10 do
        let prefix[i] <- nums[i] + last;
        let last <- prefix[i] + 2;
        let i <- i + 1;
    od;
}.

